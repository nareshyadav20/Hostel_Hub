import React from 'react';

const Page = () => {
  return (
    <div>
      <h1>Listing</h1>
      <p>Management module for Listing will be implemented here.</p>
      <div className="card" style={{ marginTop: '2rem', height: '300px', display: 'flex', alignItems: 'center', justifyContent: 'center', border: '2px dashed var(--border-color)' }}>
        <p style={{ color: 'var(--text-muted)' }}>Listing Module Content Coming Soon</p>
      </div>
    </div>
  );
};

export default Page;
